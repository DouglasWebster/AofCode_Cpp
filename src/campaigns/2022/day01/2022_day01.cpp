#include "2022_day01.hpp"

std::vector<int> collate_calories(const ElvenPacks & elven_packs)
{
  std::vector<int> elves_calories{};

  if (elven_packs.empty()) { return elves_calories; }

  return elves_calories;
}