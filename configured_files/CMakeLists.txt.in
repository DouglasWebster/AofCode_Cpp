set(path ${CMAKE_CURRENT_LIST_DIR})
cmake_path(GET path FILENAME this_directory)

if(NOT EXISTS "${CMAKE_CURRENT_LIST_DIR}/${dir_name}/${parent_directory}_${dir_name}.hpp")
    message("Creating standard header file for this days solution.")
    configure_file(
        "${CMAKE_SOURCE_DIR}/configured_files/day_details.hpp.in" 
        "${CMAKE_CURRENT_LIST_DIR}/${dir_name}/${parent_directory}_${dir_name}.hpp")
endif()

if(NOT EXISTS "${CMAKE_CURRENT_LIST_DIR}/${dir_name}/${parent_directory}_${dir_name}.cpp")
    message("Creating standard code file for this days solution.")
    configure_file(
        "${CMAKE_SOURCE_DIR}/configured_files/day_details.cpp.in" 
        "${CMAKE_CURRENT_LIST_DIR}/${dir_name}/${parent_directory}_${dir_name}.cpp")
endif()

if(NOT EXISTS "${CMAKE_CURRENT_LIST_DIR}/${dir_name}/main.cpp")
    message("Creating standard main.cpp file for this days solution.")
    configure_file(
        "${CMAKE_SOURCE_DIR}/configured_files/day_main.cpp.in" 
        "${CMAKE_CURRENT_LIST_DIR}/${dir_name}/main.cpp")
endif()


add_executable(${parent_directory}-${dir_name} main.cpp)